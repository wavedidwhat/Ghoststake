# Dependencies
node_modules/
.pnp
.pnp.*

# Build output
.next/
out/
build/
dist/
bin/
tmp/

# Secrets — never commit real values. Templates (*.example) are fine.
.env
.env.*
!.env.example
# The keeper's own file (GHO-24). It holds a private key and is deliberately
# separate from .env, so its template needs naming separately too — `.env.*`
# above would otherwise swallow it along with the real thing.
!.env.keeper.example

# Go
*.test
*.out

# Logs
*.log
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*

# Editor / OS
.DS_Store
*.pem
.idea/
*.swp

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# Personal working notes — decision log and agent instructions, local only
/docs/
/CLAUDE.md
